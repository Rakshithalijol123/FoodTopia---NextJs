import React from "react";
import HandleInput from "./HandleInput";

export const metadata = {
  title: 'Register | FoodTopia',
}

const page = () => {
  return (
    <div>
      <HandleInput />
    </div>
  );
};

export default page;
