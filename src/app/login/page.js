import React from "react";
import InputForm from "./InputForm";

export const metadata = {
  title: 'Login | FoodTopia',
}

const page = () => {
  return (
    <div>
      <InputForm />
    </div>
  );
};

export default page;
